# Public Hostname Example

Manual Cloudflare Tunnel public hostname route:

```text
Public hostname: beta.datenpflege-nord.de
Service: http://localhost:8017
```

The tunnel already exists. This file is an example only and does not configure Cloudflare.

Do not expose a local router port. Do not point the public hostname directly at Uvicorn.
