# Placeholder only. Do not commit real secrets.

AQH_WEB_HOST=127.0.0.1
AQH_WEB_PORT=8017
AQH_WEB_TOKEN=replace-with-random-private-api-token
AQH_WEB_JOBS_DIR=/app/.var/private-web/jobs
AQH_WEB_MAX_UPLOAD_MB=50
AQH_WEB_JOB_TTL_HOURS=24
AQH_WEB_MAX_ACTIVE_JOBS=1

# Preferred for the temporary private beta dashboard gate.
# Example format only; generate and store the real hash outside Git.
AQH_BETA_PASSWORD_HASH=sha256:replace-with-real-sha256-password-hash

# Alternative temporary private beta option only.
# AQH_BETA_PASSWORD=replace-with-temporary-private-beta-password
